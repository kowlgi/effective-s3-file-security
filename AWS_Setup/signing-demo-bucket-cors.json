{
    "CORSRules": [
        {
            "AllowedOrigins": ["*"],
            "AllowedMethods": ["HEAD", "GET", "PUT", "POST"],
            "MaxAgeSeconds": 3000,
            "AllowedHeaders": ["Authorization", "x-amz-*", "content-type"]
        }
    ]
}
